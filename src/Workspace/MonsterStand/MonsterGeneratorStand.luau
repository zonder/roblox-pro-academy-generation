-- LocalScript (put it inside the part that has the ProximityPrompt)

local MonsterGenerator = require(game:GetService("StarterPlayer").StarterCharacterScripts:WaitForChild("MonsterGenerator"))

-- Initialize this stand
MonsterGenerator.new(script.Parent) 